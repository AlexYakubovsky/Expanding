import styles from './Header.module.sass'

const Header = () => {
  return (
    <header
      className={styles.header}
      style={{left: '250px'}}
    >
      header
    </header>
  )
}

export default Header